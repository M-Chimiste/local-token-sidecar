# Copy this file to secrets.yaml and replace the placeholder values.
# Keep secrets.yaml local; it is ignored by git.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_ap_password: "CHANGE_ME_FALLBACK_AP_PASSWORD"
ota_password: "CHANGE_ME_OTA_PASSWORD"

# Generate with: esphome noise-key
api_encryption_key: "REPLACE_WITH_BASE64_NOISE_KEY"
